# Nuvra

Nuvra is a proof-first job agent. It reads live roles from the Speedrun Talent
Network API, keeps builder profiles in localStorage, and calls OpenRouter only
from server functions.

Keep application claims grounded in the supplied profile and send people to the
canonical Speedrun listing to view or apply for a role.
